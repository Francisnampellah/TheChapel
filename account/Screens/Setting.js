import { StyleSheet, Text, View } from "react-native";
import React from "react";
import DarkMode from "../../component/settingItem";

export default function Setting() {
  return (
    <View>
      <DarkMode />
    </View>
  );
}

const styles = StyleSheet.create({});
